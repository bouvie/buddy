{
  "name": "rules-engine",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/rules-engine/src",
  "projectType": "library",
  "tags": ["type:domain", "scope:analysis"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/libs/rules-engine"],
      "options": {
        "command": "tsc --project libs/rules-engine/tsconfig.lib.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
